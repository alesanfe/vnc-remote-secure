{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "VNC Remote Secure configuration",
  "description": "Schema validating the .env configuration for VNC Remote Secure.",
  "type": "object",
  "properties": {
    "VNC_PORT": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "description": "Port the VNC server listens on."
    },
    "NOVNC_PORT": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "description": "Port the noVNC web client listens on."
    },
    "TTYD_PORT": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "description": "Port the ttyd web terminal listens on."
    },
    "HEALTH_WEB_PORT": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "description": "Port the health check web server listens on."
    },
    "LANDING_PORT": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "description": "Port the landing page server listens on."
    },
    "TTYD_USERNAME": {
      "type": "string",
      "minLength": 3,
      "description": "Username for the ttyd web terminal."
    },
    "TTYD_PASSWD": {
      "type": "string",
      "minLength": 8,
      "description": "Password for the ttyd web terminal."
    },
    "VNC_PASSWORD": {
      "type": "string",
      "minLength": 8,
      "description": "Password for the VNC server."
    },
    "SSL_CERT": {
      "type": "string",
      "description": "Path to the SSL certificate file."
    },
    "SSL_KEY": {
      "type": "string",
      "description": "Path to the SSL private key file."
    },
    "DUCK_DOMAIN": {
      "type": "string",
      "description": "DuckDNS domain used for dynamic DNS."
    },
    "DUCKDNS_TOKEN": {
      "type": "string",
      "description": "DuckDNS API token."
    },
    "VNC_GEOMETRY": {
      "type": "string",
      "pattern": "^\\d+x\\d+$",
      "description": "VNC desktop geometry as WIDTHxHEIGHT."
    },
    "VNC_DEPTH": {
      "type": "integer",
      "enum": [8, 16, 24, 32],
      "description": "VNC color depth."
    },
    "KEEP_TEMP_USER": {
      "type": "boolean",
      "description": "Whether to keep the temporary user after the session ends."
    },
    "HEALTHCHECK_ENABLED": {
      "type": "boolean",
      "description": "Whether the health check web server is enabled."
    },
    "RECORDING_ENABLED": {
      "type": "boolean",
      "description": "Whether session recording is enabled."
    },
    "AUDIO_STREAM_ENABLED": {
      "type": "boolean",
      "description": "Whether audio forwarding is enabled."
    },
    "GAMEPAD_ENABLED": {
      "type": "boolean",
      "description": "Whether gamepad forwarding is enabled."
    },
    "USER_UI_ENABLED": {
      "type": "boolean",
      "description": "Whether the user management UI is enabled."
    },
    "WEBTERM_SHELL": {
      "type": "string",
      "description": "Shell executable used by the web terminal."
    },
    "USER_UI_PORT": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "description": "Port the user management UI listens on."
    },
    "USER_UI_PASSWORD": {
      "type": "string",
      "minLength": 8,
      "description": "Password for the user management UI."
    },
    "FLASK_SECRET_KEY": {
      "type": "string",
      "description": "Secret key for Flask session signing."
    },
    "AUTH_SECRET": {
      "type": "string",
      "description": "Secret key for JWT/token signing."
    },
    "HEALTH_AUTH_TOKEN": {
      "type": "string",
      "description": "Bearer token required to access health endpoints."
    },
    "LANDING_HOST": {
      "type": "string",
      "description": "Bind address for the landing page server."
    },
    "LANDING_PASSWORD": {
      "type": "string",
      "description": "Optional Basic-auth password for the landing page."
    },
    "TTYD_HOST": {
      "type": "string",
      "description": "Bind address for the ttyd web terminal."
    },
    "VNC_HTTP_PORT": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "description": "Port the VNC HTTP server listens on (UltraVNC)."
    },
    "USER_UI_SESSION_TIMEOUT": {
      "type": "integer",
      "minimum": 60,
      "description": "Session timeout in seconds for the user UI."
    },
    "TRUSTED_PROXY": {
      "type": "boolean",
      "description": "Whether to trust X-Forwarded-* headers from a reverse proxy."
    }
  },
  "required": [],
  "additionalProperties": true
}
