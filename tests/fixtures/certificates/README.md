# Test Certificates

This directory contains test certificates for integration tests.

**WARNING: These certificates are INSECURE and must NEVER be used in production.**

Generate test certificates with:
```bash
python scripts/utilities/generate_certificate.py --output tests/fixtures/certificates/
```
